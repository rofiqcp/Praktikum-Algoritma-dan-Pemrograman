# Deployable Flask Starter
Local: install requirements lalu `python app.py`. Production-style: `gunicorn app:app`. Uji `/` dan `/health`. Konfigurasi platform dapat berubah; cek docs resmi dan jangan commit `.env`/secret.
