# Praktikum Pertemuan 09

```bash
cd flask-web
python -m venv .venv
python -m pip install -r requirements.txt
python app.py
```

Buka `http://127.0.0.1:5000`, lalu test:
```bash
python -m unittest -v test_app.py
```
