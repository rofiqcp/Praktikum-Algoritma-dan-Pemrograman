const API='http://127.0.0.1:5000/api/products';async function load(){let r=await fetch(API),b=await r.json();document.querySelector('#list').innerHTML=(b.data||[]).map(p=>`<li>${p.name} — ${p.category_name} — Rp${p.price} — stok ${p.stock}</li>`).join('')}document.querySelector('#form').onsubmit=async e=>{e.preventDefault();let b={name:name.value.trim(),category_id:1,price:Number(price.value),stock:Number(stock.value)};let r=await fetch(API,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)}),x=await r.json();error.textContent=r.ok?'':JSON.stringify(x,null,2);if(r.ok){e.target.reset();load()}};load();
