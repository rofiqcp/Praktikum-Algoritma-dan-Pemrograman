const API='http://127.0.0.1:5000/api/products';async function load(){try{let r=await fetch(API);if(!r.ok)throw Error(`HTTP ${r.status}`);let {data}=await r.json();rows.innerHTML=data.map(p=>`<tr><td>${p.name}</td><td>${p.category_name}</td><td>${p.stock}</td></tr>`).join('');status.textContent=`${data.length} produk`}catch(e){status.textContent=`Backend error: ${e.message}`}}form.onsubmit=async e=>{e.preventDefault();let b={name:name.value.trim(),category_id:1,stock:Number(stock.value)};let r=await fetch(API,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)});if(!r.ok){status.textContent=JSON.stringify(await r.json());return}e.target.reset();load()};load();
