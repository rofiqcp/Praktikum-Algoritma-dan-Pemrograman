# Final Starter
Backend: `cd backend`, install requirements, `python test_api.py`, `python app.py`. Frontend: `cd frontend`, `npm install`, `npm start`. Buka http://localhost:3000. Starter membuktikan Node UI → Python API → SQLite dua tabel; kembangkan satu pilihan `Project.md`.
