# Materi Pertemuan 05 — List, Data, Pencarian, dan Statistik

## Target
Membuat/mengubah list, loop pada list, mencari data, menghitung min/max/sum/average, serta mengenal dictionary.

```python
nilai=[80,75,90,68,88]
print(len(nilai), min(nilai), max(nilai), sum(nilai)/len(nilai))
produk={"nama":"Keyboard","harga":250000,"stok":10}
```

List of dictionaries menjadi jembatan menuju JSON/API/database. Waspadai `IndexError`, `KeyError`, list kosong saat menghitung rata-rata, dan list yang tidak sengaja di-reset di dalam loop.
