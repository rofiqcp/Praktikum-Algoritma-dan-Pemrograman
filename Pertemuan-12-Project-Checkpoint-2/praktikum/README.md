# Praktikum Pertemuan 12 — Starter Web/API

Starter menunjukkan tiga halaman, internal REST API, dan mock external data lokal. **Jangan submit starter sebagai project akhir checkpoint.**

```bash
cd starter-web-api
python -m pip install -r requirements.txt
python app.py
python -m unittest -v test_app.py
```
